# QFN Portfolio Optimizer - Render 배포용 환경변수
# 이 파일을 .env로 복사하고 실제 값으로 변경하세요

# ===========================================
# 필수 환경변수 (반드시 설정해야 함)
# ===========================================

# PostgreSQL 데이터베이스 연결 (Render PostgreSQL Internal Database URL)
DATABASE_URL=postgres://username:password@hostname:port/database_name

# JWT 보안 키 (256비트 이상의 안전한 키)
JWT_SECRET=qfn-super-secret-jwt-key-256-bits-long-for-production-use-change-this

# Google Gemini AI API 키 (AI 챗봇 기능용)
GEMINI_API_KEY=your_google_gemini_api_key_here

# ===========================================
# 선택적 환경변수 (기능에 따라 설정)
# ===========================================

# 이메일 서비스 설정 (비밀번호 찾기 기능용)
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_gmail_app_password_here

# CORS 설정 (프론트엔드 도메인)
CORS_ALLOWED_ORIGINS=https://*.onrender.com,https://localhost:3000

# Spring Boot 프로필 (자동으로 render로 설정됨)
SPRING_PROFILES_ACTIVE=render

# 포트 설정 (Render에서 자동으로 설정됨)
PORT=10000

# ===========================================
# Render 자동 설정 환경변수 (수동 설정 불필요)
# ===========================================

# 다음 환경변수들은 Render에서 자동으로 설정됩니다:
# RENDER=true
# RENDER_SERVICE_ID=srv-xxxxx
# RENDER_SERVICE_NAME=your-service-name
# RENDER_GIT_COMMIT=commit-hash
# PYTHON_VERSION=3.10.8

# ===========================================
# 개발 환경용 추가 설정
# ===========================================

# 개발 모드에서만 사용 (프로덕션에서는 false로 설정)
DEBUG=false
SPRING_JPA_SHOW_SQL=false

# 로깅 레벨
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_PORTFOLIO_OPTIMIZER=INFO

# ===========================================
# 설정 가이드
# ===========================================

# 1. Render PostgreSQL 데이터베이스 생성:
#    - Render Dashboard → New + → PostgreSQL
#    - Database Name: qfn-portfolio-db
#    - 생성 후 Internal Database URL을 DATABASE_URL에 설정

# 2. Google Gemini API 키 발급:
#    - https://makersuite.google.com/app/apikey
#    - API 키 생성 후 GEMINI_API_KEY에 설정

# 3. Gmail 앱 패스워드 생성 (선택사항):
#    - Gmail 계정 → 보안 → 2단계 인증 활성화
#    - 앱 패스워드 생성 → MAIL_PASSWORD에 설정

# 4. JWT 보안 키 생성:
#    - 온라인 JWT 시크릿 제너레이터 사용 또는
#    - 32자 이상의 랜덤 문자열 생성

# 5. Render 웹 서비스 환경변수 설정:
#    - 서비스 Settings → Environment → Add Environment Variable
#    - 위의 필수 환경변수들을 하나씩 추가